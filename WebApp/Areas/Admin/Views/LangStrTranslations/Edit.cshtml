@model WebApp.Areas.Admin.ViewModels.LangStrTranslationViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Language string translation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">

            <partial name="_CreateEdit" model="@Model"></partial>

            <input type="hidden" asp-for="LanguageStringTranslation.Id" />
            
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
